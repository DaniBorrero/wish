<%= form_with(model: @event, local: true) do |form| %>
<% if @event.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

  <ul>
    <% event.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="form-group my-5">
  <%= form.label :name, "Nombre del evento", class: "col-lg-2 control-label" %>
  <div class="col-lg-10">
    <%= form.text_field :name, id: :product_name %>
  </div>
</div>

<div class="form-group my-5">
  <%= form.label :description, "Descripcion", class: "col-lg-2 control-label" %>
  <div class="col-lg-10">
    <%= form.text_field :description, id: :product_name %>
  </div>
</div>

<div class="form-group my-5">
  <%= form.label :strat_date, "Fecha de inicio", class: "col-lg-2 control-label" %>
  <div class="col-lg-10">
    <%= form.text_field :strat_date, id: :product_name %>
  </div>
</div>

<div class="actions">
  <%= form.submit %>
</div>
<% end %>